export type ExecutionMode='ROUTED'|'SPECIFIC'|'FULL_PANEL';
export type AgentIR={version:2;op:'AGENT_EXECUTE';agent:string;subjectId:string;unitId:string;conversationId:string;context:string;memoryScope:string;maxOutputLines:number;grounded:boolean};
export function compileAgentRequest(input:{agent:string;subjectId:string;unitId?:string;conversationId:string;context?:string;mode?:ExecutionMode}):AgentIR{if(!input.subjectId||!input.conversationId)throw new Error('INVALID_SCOPE');if(!/^[a-z0-9_-]+$/i.test(input.agent))throw new Error('INVALID_AGENT');const unitId=input.unitId||'unspecified';return {version:2,op:'AGENT_EXECUTE',agent:input.agent,subjectId:input.subjectId,unitId,conversationId:input.conversationId,context:(input.context||'').slice(0,16000),memoryScope:`user-scoped:${input.subjectId}:${input.conversationId}:${input.agent}`,maxOutputLines:input.agent==='idsanna'?10:8,grounded:true}}
export function compilePanel(agents:string[],scope:{subjectId:string;unitId?:string;conversationId:string;context?:string}){if(!agents.length||agents.length>7)throw new Error('INVALID_PANEL');return agents.map(agent=>compileAgentRequest({agent,...scope}))}
